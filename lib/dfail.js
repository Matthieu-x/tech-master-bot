/**
 * lib/dfail.js
 * -------------------------------------------------------
 * Helper para formatear mensajes de error/aviso con un
 * estilo consistente en todo el bot.
 * -------------------------------------------------------
 */
function dfail(texto) {
  return `ꕥ\n> ${texto}`
}

module.exports = dfail
